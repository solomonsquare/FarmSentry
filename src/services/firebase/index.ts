import { db, auth, analytics } from './config';
import * as collections from './collections';
import * as schema from './schema';
import * as operations from './operations';

export {
  db,
  auth,
  analytics,
  collections,
  schema,
  operations
};